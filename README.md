# WireMock интеграция для E2E автотестов на Cucumber

Простая и понятная интеграция WireMock в проект с Cucumber тестами для перехвата и мокирования API ручек с поддержкой параллельного выполнения тестов.

## 📋 Содержание

- [Быстрый старт](#быстрый-старт)
- [Структура проекта](#структура-проекта)
- [Использование](#использование)
  - [В feature-файлах](#в-feature-файлах)
  - [В step definitions](#в-step-definitions)
  - [Программное использование](#программное-использование)
- [Параллельное выполнение](#параллельное-выполнение)
- [Примеры](#примеры)
- [FAQ](#faq)

## 🚀 Быстрый старт

### 1. Установка зависимостей

Проект использует Maven. Все зависимости уже настроены в `pom.xml`:
- WireMock 3.9.1
- Cucumber 7.20.1
- JUnit 5

```bash
mvn clean install
```

### 2. Запуск примера

```bash
# Запуск всех тестов
mvn test

# Запуск конкретного feature-файла
mvn test -Dcucumber.features=src/test/resources/features/wiremock-example.feature

# Запуск с конкретным тегом
mvn test -Dcucumber.filter.tags="@wiremock"

# Параллельный запуск (4 потока)
mvn test -Dparallel=methods -DthreadCount=4
```

### 3. Проверка работы

После запуска в логах вы увидите:
```
INFO WireMockManager - WireMock сервер запущен: http://localhost:XXXXX (поток: main)
INFO MockSteps - Создание мока: GET /api/users -> 200
```

## 📁 Структура проекта

```
src/test/java/
├── wiremock/
│   ├── WireMockManager.java      # Менеджер WireMock серверов (ThreadLocal)
│   └── MockBuilder.java          # Утилита для создания моков (Fluent API)
├── hooks/
│   └── WireMockHooks.java        # Cucumber хуки (@Before/@After)
├── stepdefs/
│   └── MockSteps.java            # Готовые step definitions
└── runners/
    ├── CucumberTestRunner.java   # JUnit 5 test runner
    └── ParallelStrategy.java     # Настройки параллельного запуска

src/test/resources/
├── features/
│   └── wiremock-example.feature  # Примеры использования
└── responses/
    ├── user.json                 # Примеры JSON ответов
    └── login-success.json
```

## 💡 Использование

### В feature-файлах

Самый простой способ - использовать готовые step definitions:

```gherkin
# language: ru
@wiremock
Функция: Тестирование с моками

  Сценарий: Успешный вход пользователя
    # Простой GET с кастомным статусом
    Дано API endpoint "/api/users" returns status 200
    
    # GET с JSON телом
    Дано API endpoint "/api/users/1" returns status 200 with JSON:
      """
      {"id": 1, "name": "Иван", "email": "ivan@test.com"}
      """
    
    # POST с JSON
    Дано POST endpoint "/api/login" returns status 200 with JSON:
      """
      {"token": "abc123", "userId": 1}
      """
    
    # Задержка для тестирования таймаутов
    Дано API endpoint "/api/slow" returns status 408 with 5000ms delay
    
    # JSON из файла
    Дано API endpoint "/api/user" returns JSON from file "responses/user.json"
    
    # Русскоязычные шаги
    Когда мокирую GET запрос "/api/data" со статусом 200 и телом "OK"
```

### В step definitions

Использование `MockBuilder` для более сложных сценариев:

```java
import wiremock.MockBuilder;
import static com.github.tomakehurst.wiremock.client.WireMock.*;

@When("пользователь загружает список продуктов")
public void загрузкаСпискаПродуктов() {
    // Простой мок
    MockBuilder.get("/api/products")
        .withStatus(200)
        .withJsonBody("[{\"id\": 1, \"name\": \"Товар 1\"}]")
        .stub();
    
    // Ваша логика UI теста
    // driver.get(baseUrl + "/products");
}

@When("пользователь создает новый заказ")
public void созданиеНовогоЗаказа() {
    // POST с проверкой тела запроса
    MockBuilder.post("/api/orders")
        .withRequestBody(containing("productId"))
        .withStatus(201)
        .withJsonBody("{\"orderId\": \"12345\"}")
        .stub();
    
    // Ваша логика UI теста
}

@When("пользователь обращается к защищенному ресурсу")
public void обращениеКЗащищенномуРесурсу() {
    // Проверка заголовка Authorization
    MockBuilder.get("/api/profile")
        .withRequestHeader("Authorization", containing("Bearer"))
        .withStatus(200)
        .withJsonBodyFromFile("responses/user.json")
        .stub();
}
```

### Программное использование

Прямое использование `WireMockManager`:

```java
import wiremock.WireMockManager;

@Before
public void setup() {
    // Получить базовый URL WireMock сервера
    String mockServerUrl = WireMockManager.getBaseUrl();
    
    // Настроить ваше приложение на использование мок-сервера
    System.setProperty("api.base.url", mockServerUrl);
}

@After
public void teardown() {
    // Хуки уже автоматически останавливают сервер,
    // но можно сделать это и вручную:
    WireMockManager.stopServer();
}
```

## ⚡ Параллельное выполнение

### Как это работает

**ThreadLocal изоляция**: Каждый поток получает свой собственный WireMock сервер на уникальном порту. Это обеспечивает полную изоляцию между параллельными тестами.

### Настройка параллелизма

В `pom.xml` уже настроен Maven Surefire для параллельного запуска:

```xml
<plugin>
    <groupId>org.apache.maven.plugins</groupId>
    <artifactId>maven-surefire-plugin</artifactId>
    <configuration>
        <parallel>methods</parallel>
        <threadCount>4</threadCount>
    </configuration>
</plugin>
```

Изменить количество потоков можно:
1. В `pom.xml` → измените `<threadCount>`
2. В `runners/ParallelStrategy.java` → измените `getParallelism()`
3. Через командную строку: `mvn test -DthreadCount=8`

### Проверка параллельности

В логах вы увидите разные порты для каждого потока:

```
INFO WireMockManager - WireMock сервер запущен: http://localhost:52341 (поток: pool-1-thread-1)
INFO WireMockManager - WireMock сервер запущен: http://localhost:52342 (поток: pool-1-thread-2)
INFO WireMockManager - WireMock сервер запущен: http://localhost:52343 (поток: pool-1-thread-3)
```

## 📚 Примеры

### Пример 1: Мокирование успешного логина

```gherkin
Сценарий: Успешный вход в систему
  Дано POST endpoint "/api/auth/login" returns status 200 with JSON:
    """
    {"token": "jwt_token_here", "userId": 1}
    """
  Когда пользователь вводит логин "admin" и пароль "password"
  И нажимает кнопку "Войти"
  Тогда должен увидеть дашборд администратора
```

### Пример 2: Мокирование 404 ошибки

```gherkin
Сценарий: Пользователь не найден
  Дано API endpoint "/api/users/999" returns status 404
  Когда пользователь открывает профиль с ID 999
  Тогда должен увидеть сообщение "Пользователь не найден"
```

### Пример 3: Сложный мок с матчингом

```java
MockBuilder.post("/api/search")
    .withRequestBody(matchingJsonPath("$.query"))
    .withRequestHeader("Content-Type", containing("json"))
    .withQueryParam("page", equalTo("1"))
    .withStatus(200)
    .withJsonBody("{\"results\": [], \"total\": 0}")
    .stub();
```

### Пример 4: Использование файлов для ответов

```gherkin
Сценарий: Загрузка данных из файла
  Дано API endpoint "/api/users" returns JSON from file "responses/users-list.json"
  Когда пользователь открывает список пользователей
  Тогда должен увидеть 5 пользователей
```

## ❓ FAQ

### Как изменить базовый URL API в моих тестах?

```java
@Before
public void setup() {
    String mockUrl = WireMockManager.getBaseUrl();
    // Используйте mockUrl как базовый URL вашего API клиента
    apiClient.setBaseUrl(mockUrl);
}
```

### Моки не работают, что проверить?

1. Проверьте логи - сервер должен запуститься:
   ```
   INFO WireMockManager - WireMock сервер запущен: http://localhost:XXXXX
   ```

2. Убедитесь что ваше приложение обращается к мок-серверу, а не к реальному API

3. Проверьте, что URL в моках совпадает с URL запросов

### Как отладить запросы к WireMock?

Добавьте в `src/test/resources/simplelogger.properties`:

```properties
org.slf4j.simpleLogger.log.wiremock=DEBUG
org.slf4j.simpleLogger.log.WireMockManager=DEBUG
```

### Можно ли использовать regex в URL?

Да, используйте `MockBuilder.getMatching()`:

```java
MockBuilder.getMatching("/api/users/[0-9]+")
    .withStatus(200)
    .stub();
```

### Как проверить что запрос был сделан?

```java
import static com.github.tomakehurst.wiremock.client.WireMock.*;

// После выполнения теста
verify(getRequestedFor(urlEqualTo("/api/users")));
verify(postRequestedFor(urlEqualTo("/api/login"))
    .withRequestBody(containing("username")));
```

### Как сбросить все моки в середине теста?

```java
WireMockManager.resetMappings();
```

### Сервер не останавливается после теста

Хуки `WireMockHooks` автоматически останавливают сервер. Убедитесь, что:
1. Класс `WireMockHooks` находится в пакете `hooks`
2. В `CucumberTestRunner` указан glue: `stepdefs,hooks`

---

## 🎯 Основные преимущества

✅ **Максимальная простота** - минимум кода, работает из коробки  
✅ **Thread-safe** - полная поддержка параллельного запуска  
✅ **Cucumber-friendly** - готовые steps для feature-файлов  
✅ **Гибкость** - можно использовать декларативно или программно  
✅ **Java 23 compatible** - последние версии всех библиотек  
✅ **Zero configuration** - автоматическое выделение портов

## 📝 Лицензия

MIT
